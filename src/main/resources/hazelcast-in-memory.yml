hazelcast:
  cluster-name: "in-memory"
  map:
    time-cache:
      time-to-live-seconds: 10
    time-cache-locks:
      time-to-live-seconds: 30
  network:
    join:
      multicast:
        enabled: false
      tcp-ip:
        enabled: false
